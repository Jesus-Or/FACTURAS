<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard Mes a Mes</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', Arial, sans-serif;
      background: white;
    }

    h1 {
      color: #260574;
      font-weight: 500;
      margin: 2rem 0 1rem 0;
    }

    .badge {
      font-size: 1em;
    }

    .chart-container {
      max-width: 900px;
      margin: auto;
    }
  </style>
</head>

<body>
  <div class="container py-4">

    <h1>Seguimiento Facturas Mes a Mes</h1>
    <a href="/extraerfacturapdf" class="btn btn-outline-primary mb-3">Volver a Facturas</a>

    <div class="table-responsive mb-4">
      <table class="table table-bordered table-hover align-middle">
        <thead class="table-primary">
          <tr>
            <th>Mes</th>
            <th>Monto Total</th>
            <th>Comparación</th>
            <th>Estado</th>
          </tr>
        </thead>
        <tbody>
          <% for(let i=0; i<datos.length; i++) { const actual=datos[i].TotalMes; const anterior=i> 0 ?
            datos[i-1].TotalMes : null;
            let cambio = null, estado = '';
            if(anterior !== null) cambio = actual - anterior;
            if(anterior !== null) {
            if(cambio > 0) estado = 'Subió';
            else if(cambio < 0) estado='Bajó' ; else estado='Estable' ; } %>
              <tr>
                <td>
                  <%= datos[i].Mes %>
                </td>
                <td class="text-end">
                  <%= actual.toLocaleString('es-CO', {minimumFractionDigits:2}) %>
                </td>
                <td>
                  <% if(anterior !==null) { %>
                    <%= (cambio> 0 ? '+' : '') + cambio.toLocaleString('es-CO', {minimumFractionDigits:2}) %>
                      <% } %>
                </td>
                <td>
                  <% if(anterior !==null) { %>
                    <% if(cambio> 0) { %>
                      <span class="badge bg-success">⬆ Subió</span>
                      <% } else if(cambio < 0) { %>
                        <span class="badge bg-danger">⬇ Bajó</span>
                        <% } else { %>
                          <span class="badge bg-secondary">= Estable</span>
                          <% } %>
                            <% } %>
                </td>
              </tr>
              <% } %>
        </tbody>
      </table>
    </div>

  </div>


</body>

</html>