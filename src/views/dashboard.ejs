<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Dashboard Mes a Mes</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">
  <a href="/extraerfacturapdf" class="btn btn-outline-primary m-3">← Volver</a>

  <div class="container mt-4">
    <h2 class="text-center mb-4">📊 Estado Mes a Mes de Facturas</h2>

    <table class="table table-bordered text-center align-middle">
      <thead class="table-dark">
        <tr>
          <th>No. Factura</th>
          <th>Nombre del Proveedor</th>
          <th>Mes</th>
          <th>Total Mes</th>
          <th>Estado</th>
        </tr>
      </thead>
      <tbody>
        <% datos.forEach(d => { 
          const mapaClientes = {
            "Global IT Services - CERT (Digital Security)": "GIT-CERT",
            "Mark-up 5% - CERT (Digital Security)": "MU-CERT",
            "Global Guarding Platform - TrackTik": "GGP-TrackTik",
            "Global IT Services - Digital Identity": "GIT-DI",
            "Mark-up Digital Identity": "MU-DI",
            "Global IT Services - LMS": "GIT-LMS",
            "Mark-up LMS": "MU-LMS",
            "Global IT Services - Workplace from Meta": "GIT-WFM",
            "Mark-up Workplace from Meta": "MU-WFM"
          };

          const clienteReducido = mapaClientes[d.Cliente] || d.Cliente;
        %>
          <tr>
            <td><%= d.NumeroFactura %></td>
            <td><%= clienteReducido %></td>
            <td><%= d.Mes %></td>
            <td>$<%= d.TotalMes.toLocaleString() %></td>
            <td class="<%= 
                d.Estado === 'Subió' ? 'text-success fw-bold' : 
                d.Estado === 'Bajó' ? 'text-danger fw-bold' : 
                'text-secondary fw-bold' %>">
              <%= d.Estado %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
